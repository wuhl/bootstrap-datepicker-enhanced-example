<h1><%= t('helpers.titles.edit', model: Project.model_name.human ) %></h1>

<%= render 'form' %>

<%= link_to t('helpers.links.show'), @project %> |
<%= link_to t('helpers.links.back'), projects_path %>